<?php

$s = '';

$s.= '<aside>'.N;
$s.= '  <div class="info">'.N;
if ($this->tutorial->image) {
    $s.= '    <div class="image">'.N;
    $s.= '      <img src="/img/tutorials/' . $this->tutorial->image . '" alt="' . $this->tutorial->title . '" title="' . $this->tutorial->title . '">'.N;
    $s.= '    </div>'.N;
}
$s.= '    <ul>'.N;
$s.= '      <li><strong>' . $this->tutorial->title . '</strong></li>'.N;
if ($this->user) {
    $s.= '      <li>'.N;
    $href = $this->url('tutorials/edit', array('slug' => $this->tutorial->slug));
    $s.= '        <a href="' . $href . '">Edit</a>'.N;
    $s.= '      </li>'.N;
    $s.= '      <li>'.N;
    $href = $this->url('tutorials/delete', array('slug' => $this->tutorial->slug)) . '?token=' . $this->csrfToken;
    $s.= '        <a href="' . $href . '" class="delete" data-prompt="Are you sure you want to delete this tutorial?">Delete</a>'.N;
    $s.= '      </li>'.N;
}
$s.= '    </ul>'.N;
$s.= '  </div>'.N;
if ($this->tutorial->intro) {
    $s.= '  <div class="intro">'.N;
    $s.= '    <p>' . $this->tutorial->intro . '</p>'.N;
    $s.= '  </div>'.N;
}
$s.= '</aside>'.N;

print($s);